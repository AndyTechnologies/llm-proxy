---
import AppLayout from "../../layouts/AppLayout.astro";
import EmptyState from "../../svelte/common/EmptyState.svelte";
---

<AppLayout title="Providers" breadcrumb={["Providers"]}>
  <EmptyState
    icon="providers"
    title="Provider connections"
    description="External providers (openai, anthropic, openrouter) and their fallback links appear here. Data comes from the backend providers API — not available yet, the typed client connects in a later phase."
  />
</AppLayout>