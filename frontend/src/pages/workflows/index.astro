---
import AppLayout from "../../layouts/AppLayout.astro";
import EmptyState from "../../svelte/common/EmptyState.svelte";
---

<AppLayout title="Workflows" breadcrumb={["Workflows"]}>
  <EmptyState
    icon="workflows"
    title="Workflow library"
    description="Stored workflow definitions and their run controls appear here. Data comes from the backend workflow API — not available yet, the typed client connects in a later phase."
  />
</AppLayout>