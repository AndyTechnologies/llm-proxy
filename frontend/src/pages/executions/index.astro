---
import AppLayout from "../../layouts/AppLayout.astro";
import EmptyState from "../../svelte/common/EmptyState.svelte";
---

<AppLayout title="Executions" breadcrumb={["Executions"]}>
  <EmptyState
    icon="executions"
    title="Workflow executions"
    description="Workflow run logs appear here, grouped by workflow. Data comes from the backend workflow logs — not available yet, the typed client connects in a later phase."
  />
</AppLayout>