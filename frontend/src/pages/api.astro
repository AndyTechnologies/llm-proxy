---
import AppLayout from "../layouts/AppLayout.astro";
import EmptyState from "../svelte/common/EmptyState.svelte";
---

<AppLayout title="API" breadcrumb={["API"]}>
  <EmptyState
    icon="api"
    title="API playground"
    description="OpenAI-compatible requests against /v1 (chat completions, completions, embeddings) are exercised here. The playground connects in a later phase."
  />
</AppLayout>